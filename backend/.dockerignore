__pycache__
*.py[cod]
*$py.class
.pytest_cache
.mypy_cache
.ruff_cache
.env
.env.*
*.md
.git
.gitignore

# Local-build only: none of this is in git, but without it `docker compose build
# api` uploads the 175MB backend/venv tree as build context on every run, and
# could bake a developer's local SQLite file into the image.
venv/
.venv/
data/
